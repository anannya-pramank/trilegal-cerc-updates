requests
beautifulsoup4

# PDF parsing: pymupdf4llm extracts table-aware Markdown (preferred); it pulls in
# pymupdf. pdfplumber is kept as an automatic fallback parser.
pymupdf4llm
pdfplumber

# Local embedding model for relevance-ranked digests (the watcher approach).
# Runs on CPU on the runner; no API key, no data leaves the machine. Pulls in
# torch, so the install is heavier — the workflow caches pip + the model so only
# the first run is slow.
sentence-transformers
numpy

# Postgres driver for the Supabase loader (load_cerc.py).
psycopg2-binary
